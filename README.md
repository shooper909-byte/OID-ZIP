# OID-ZIP
Take ownership of bringing the OligoPoly Intelligence Database (OID) v1 repository to a verified deployable state. Read README.md and PRODUCTION_GATE.md first. Install dependencies, validate the Prisma schema, generate the Prisma client, provision or connect a development PostgreSQL database, run all migrations, type checks, tests, and the Next.js 
